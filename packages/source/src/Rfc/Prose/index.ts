// Values an RFC does not tabulate.
//
// Not everything worth extracting is laid out. The small constants — a
// generator, a key length, a version number — are stated in a sentence, and the
// sentence outlives the section numbering around it. `Labelled` reads those.

export { labelled } from "./Labelled.ts"
